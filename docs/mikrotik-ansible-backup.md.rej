--- docs/mikrotik-ansible-backup.md
+++ docs/mikrotik-ansible-backup.md
@@ -18,13 +18,19 @@
 - Python 3 and pipx-capable package support on the control host.
 - Day 1 bootstrap tasks have been run through the repository task workflow.
 - `task secrets:prepare` has created the age identity file used by SOPS.
-- Ansible collections installed with:
+- Ansible collections installed once with:
 
 ```bash
 task ansible:collections
 ```
 
-`task router:deps` remains available as a router-domain alias.
+Python libraries required by RouterOS SSH modules are installed into the Ansible pipx environment with:
+
+```bash
+task ansible:python-deps
+```
+
+`task router:deps` remains available as a router-domain alias for collection setup. `task router:backup` does not reinstall Galaxy collections on each backup run.
 
 ## First backup
 
